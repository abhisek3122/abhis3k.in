<!--
  ┌─────────────────────────────────────────────────────────────────┐
  │  NOTE TEMPLATE — copy this folder, don't edit it in place.        │
  └─────────────────────────────────────────────────────────────────┘

  BEFORE PUBLISHING — checklist:
   1. Copy this whole folder into  notes/<your-slug>/
        (slug = lowercase-with-hyphens; it becomes the URL /notes/<your-slug>/)
   2. Set the date below (or delete the line to use today's file date).
   3. Replace the "# Title" line with your real title.
   4. Write your content. Drop any images INTO the same folder and
      reference them by bare filename:  ![alt](photo.jpg)  (no paths).
   5. From the project root run:   node build.mjs
   6. Preview locally:             python3 -m http.server 8000
        → open  http://localhost:8000/notes/<your-slug>/
   7. Looks good? Commit + copy the site over and push.

  Notes are informal — no subtitle needed (that's a blog thing).
-->

<!-- date: 2026-01-01 -->
# Title of the note

Start writing here. Notes are casual — a thought, a moment, a small reflection.

You can use **bold**, *italic*, `inline code`, and [links](https://example.com).

- bullet one
- bullet two

> A quote, if you like.

![describe the image](photo.jpg)
